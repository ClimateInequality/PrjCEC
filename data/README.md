# Data folder

The data folder contains data files that we generated based on underlying raw data files.  These data files are inputs that scripts load to generate data/results in the `data-res` folder.

See: https://github.com/ClimateInequality/PrjCEC/issues/8
